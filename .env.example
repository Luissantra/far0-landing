# Public origin of this landing (used for metadata URLs).
# Vercel's VERCEL_PROJECT_PRODUCTION_URL is used automatically when available.
NEXT_PUBLIC_SITE_URL=

# Absolute HTTP(S) URL of the separately hosted operator dashboard.
# Leave empty to show an unavailable dashboard CTA rather than a broken link.
DASHBOARD_URL=

# Optional URL for the footer's source-code link.
REPOSITORY_URL=
